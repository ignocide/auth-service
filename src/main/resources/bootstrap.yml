server:
  port: ${PORT:8001}

spring:
  application:
    name: auth-service
  cloud:
    config:
      uri: ${CONFIG_SERVICE_URI:http://localhost:8888}
      username: ${CONFIG_SERVER_ID}
      password: ${CONFIG_SERVER_PASSWORD}
      name: auth-service, ureka
